// Placeholder for register page specific JS
// You can add interactive behavior here if needed.

// Example: auto-focus the first input field when the page loads
window.addEventListener('DOMContentLoaded', () => {
  const firstInput = document.querySelector('form input, form textarea, form select');
  if (firstInput) {
    firstInput.focus();
  }
});
